我感觉从设计的角度来说，这里应该是对业务而言，只是调用了moneyapi里面的操作，它不关注oss的类型，它有一个业务操作类型，作为moneyapi的参数传入，
然后在money模块里面会有一个映射表，就是操作类型与oss类型的映射关系，或者操作类型就是oss的日志类型
这样调用的依赖关系变成：
业务=》money=》oss，那两行代码变成actor_add_money一个调用，并传入操作类型

现在是业务同时 依赖oss和money，对业务而言只是想操作一下money而已，不应该需要关注oss_log的类型，也不应该需要依赖oss模块

